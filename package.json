{
	"name": "react-redux-node-clean",
	"version": "0.1.0",
	"description": "React Redux Node Express Clean Boilerplate",
	"main": "server.js",
	"homepage": "https://wayncheng.github.io/tft-node",
	"scripts": {

		"predeploy": "npm run build",
		"deploy": "gh-pages -d build",
		"start": "node server.js",
		"start:hot": "nodemon server.js",
		"client": "cd client && yarn start",
		"server": "cd client && yarn build && cd .. && yarn start",
		"initiate": "yarn install && touch .env && cd client && yarn install"
	},
	"author": "Wayne Cheng",
	"license": "Apache-2.0",
	"dependencies": {
		"body-parser": "^1.18.3",
		"dotenv": "^6.1.0",
		"express": "^4.16.4",
		"prop-types": "^15.7.2"
	},
	"devDependencies": {
		"chalk": "^3.0.0",
		"gh-pages": "^2.1.1",
		"nodemon": "^1.18.7"
	}
}
